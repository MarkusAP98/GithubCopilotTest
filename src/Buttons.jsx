import React, {useState} from 'react'

// i want to make this a button that when clicked it will change the state of the week and day to true or false and then be able to display that data on a different page. also i want the button class to change when clicked to show that it is clicked



const Buttons = () => {
    const [week1, setWeek1] = useState(false) {
        
    }
  return (
    <div>Buttons</div>
  )
}

export default Buttons